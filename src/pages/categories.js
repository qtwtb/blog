import React from "react"
import Layout from "../components/layout"
import { graphql, StaticQuery } from "gatsby"

export default ({ data }) => {
    
    return (
        <div>
            categories.js
        </div>
    )
}